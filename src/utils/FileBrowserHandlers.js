export function handleOpenFiles(params) {
    const { data, files, setFiles, folderChain, setFolderChain } = params
}

export function handleDeleteFiles(params) {
    const { data, files, setFiles, folderChain, setFolderChain } = params
}

export function handleCreateFolder(params) {
    const { data, files, setFiles, folderChain, setFolderChain } = params
}